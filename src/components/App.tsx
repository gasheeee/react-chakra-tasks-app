import React, { FC } from 'react';
import Header from "./header";

const App: FC = () => {
  return (
    <div className="App">
      <header className="header">
        <Header></Header>
      </header>
    </div>
  );
};

export default App;
