import React, { FC } from 'react';
import { Box } from '@chakra-ui/react';
import { Header } from '../molecules/Header';

export const App: FC = () => {
  return (
    <Box>
      <Header></Header>
    </Box>
  );
};
